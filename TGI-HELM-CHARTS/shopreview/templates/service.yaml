apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopreview.fullname" . }}
  labels:
    {{- include "shopreview.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopreview.selectorLabels" . | nindent 4 }}
